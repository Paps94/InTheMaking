var svg = document.getElementsByClassName('svgBackground');

//Making sure the SVG always takes the full screen regardless of the device
svg.width = window.innerWidth;
svg.height = window.innerHeight;
